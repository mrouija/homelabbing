in Proxmox
Select the Servername (in Server View), then select "Hardware" in the main tab, select "Hard Disk (sata0)" then click "Resize Disk"
Increment size as desired, then click "Resize Disk"
use 'fdisk -l' to find disk name (typically /dev/sda)
use 'fdisk /dev/sda' to create a new partition of type LVM
